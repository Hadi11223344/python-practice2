# calling sort() mutates the list, returns nothing 

warm = ['red', 'blue', 'green', 'yellow']
sorted_warm = warm.sort()
print(warm)
print(sorted_warm) # It prints None as the sort() method mutates the list, returns nothing

